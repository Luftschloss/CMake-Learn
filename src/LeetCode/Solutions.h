#pragma once
#include "Datas.h"

namespace LeetCode
{
    class Solutions {
    public:
        static ListNode* addTwoNumbers(ListNode* l1, ListNode* l2);

        static int divide(int dividend, int divisor);
    };
}